// Clean TypeScript file with no linting or formatting issues

function greet(name: string): string {
  return `Hello, ${name}!`
}

function add(a: number, b: number): number {
  return a + b
}

export { greet, add }
